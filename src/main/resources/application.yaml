spring:
  redis:
    host: 192.168.3.51
    port: 12000
#    username: adminn
    password: 1111
    lettuce:
      pool:
        max-active: 20
        max-idle: 10
        min-idle: 5

logging:
  level:
    # Lettuce 핵심 코어 로그를 DEBUG 또는 TRACE로 설정
    io.lettuce.core: DEBUG
    io.lettuce.core.protocol: TRACE
    # Spring Data Redis의 리다이렉션 처리 로그
    org.springframework.data.redis: DEBUG